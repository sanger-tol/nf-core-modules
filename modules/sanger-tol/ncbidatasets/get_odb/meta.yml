# yaml-language-server: $schema=https://raw.githubusercontent.com/nf-core/modules/master/modules/meta-schema.json
name: "NCBIDATASETS_GET_ODB"
description: |
  A tool to collect ODB lineages using NCBI Datasets summaries.
keywords:
  - assembly
  - summary
  - ncbi_datasets
  - get_odb
tools:
  - "get_odb":
      description: |
        Generate a CSV containing relevant ODB's to run for a given assembly.
      homepage: "https://github.com/sanger-tol/nf-core-modules"
      documentation: "https://github.com/sanger-tol/nf-core-modules"
      tool_dev_url: "https://github.com/sanger-tol/nf-core-modules"
      licence: ["MIT"]
input:
  - - meta:
        type: map
        description: |
          Groovy Map containing sample information
          e.g. `[ id:'sample1' ]`
    - ncbi_summary:
        type: file
        description: Assembly summary file
        pattern: "*.json"
        ontologies:
          - edam: "http://edamontology.org/format_3464" # JSON
  - lineage_tax_ids:
      type: string
      description: Whether the user wants to use odb10 or odb12.
      pattern: "^(odb10|odb12)$"
  - all_ancestral_lineages:
      type: boolean
      description: Whether the user wants to use all ancestral lineages or just best fit.
output:
  csv:
    - - meta:
          type: map
          description: |
            Groovy Map containing sample information
            e.g. `[ id:'sample1' ]`
      - "*.busco_odb.csv":
          type: file
          description: JSON file containing assembly statistics
          pattern: "*.busco_odb.csv"
          ontologies:
            - edam: "http://edamontology.org/format_3307" # CSV
  versions_python:
    - - ${task.process}:
          type: string
          description: Name of the process
      - python:
          type: string
          description: Name of the tool
      - python --version | sed "s/Python //":
          type: eval
          description: The expression to obtain the version of the tool
  versions_get_odb:
    - - ${task.process}:
          type: string
          description: Name of the process
      - get_odb.py:
          type: string
          description: Name of the tool
      - get_odb.py --version | cut -d" " -f2:
          type: eval
          description: The expression to obtain the version of the tool

topics:
  versions:
    - - ${task.process}:
          type: string
          description: Name of the process
      - python:
          type: string
          description: Name of the tool
      - python --version | sed "s/Python //":
          type: eval
          description: The expression to obtain the version of the tool
    - - ${task.process}:
          type: string
          description: Name of the process
      - get_odb.py:
          type: string
          description: Name of the tool
      - get_odb.py --version | cut -d" " -f2:
          type: eval
          description: The expression to obtain the version of the tool
authors:
  - "@DLBPointon"
maintainers:
  - "@DLBPointon"
