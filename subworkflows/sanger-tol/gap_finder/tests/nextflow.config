process {
    withName: GAWK {
        ext.args2   = { "${params.gawk_args}" }
        ext.suffix  = 'gap.bedgraph'
    }

    withName: SEQTK_CUTN {
        ext.args        = { "${params.seqtk_args}" }
    }
}
