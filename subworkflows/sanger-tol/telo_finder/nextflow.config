nextflow.enable.moduleBinaries = true

process {

    withName: FINDTELOMERE {
        tag        = { "${meta.id}" }
        ext.args3  = "99.9 0.05"
        ext.prefix = { "${meta.id}" }
    }

    withName: TABIX_BGZIPTABIX {
        ext.args2 = "--csi"
    }
}
