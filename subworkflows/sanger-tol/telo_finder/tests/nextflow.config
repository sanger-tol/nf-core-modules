process {
    withName: FINDTELOMERE {
        tag         = { "${meta.id}" }
        ext.args    = { params.telomerewindow_args }
        ext.prefix  = { "${meta.id}" }
    }

    withName: TABIX_BGZIPTABIX {
        ext.args2 = { "${params.bgzip_args}" }
    }
}
