nextflow.enable.moduleBinaries = true

process {
    withName: TELOMERE_WINDOWS {
        tag         = { "${meta.id}_${meta.direction ?: 'FULL'}" }
        ext.args    = { "${params.windows_percent}" }
        ext.prefix  = { "${meta.id}_${meta.direction}" }
    }

    withName: TELOMERE_EXTRACT {
        tag         = { "${meta.id}_${meta.direction ?: 'FULL'}" }
    }

    withName: GAWK {
        ext.args2   = { "${params.gawk_args}" }
        ext.suffix  = "split.telomere"
    }
}
